// Insert here all the hardcoded string that could be mispelled and lead to bugs.

/** Use this only inside the AuthProvider to keep a good level of encapsulation. */
export const localStorageKeys = {
  CUSTOMER_TOKEN: "CUSTOMER_TOKEN",
  CUSTOMER_TOKEN_EXPIRES_AT: "CUSTOMER_TOKEN_EXPIRES_AT",
};
