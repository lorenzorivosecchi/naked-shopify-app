module.exports = {
  env: {
    shopifyStorefrontApiURL: process.env.SHOPIFY_STOREFRONT_API_URL,
    // This token will be available to the browser (it's ok)
    shopifyStorefrontApiToken: process.env.SHOPIFY_STOREFRONT_API_TOKEN,
  },
};
